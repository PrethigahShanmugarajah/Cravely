// Craverly / Client / src / pages / Cart / Cart.jsx
import CartPage from "../../components/CartPage/CartPage";

const Cart = () => {
  return (
    <>
      <CartPage />
    </>
  );
};

export default Cart;
